# ai-credentials.txt — credentials for the runAiAgent build agent (KEY=value, one per line).
# Lines starting with '#' are ignored. This file is read by the AiAgentPlugin.
# No real secrets are stored here by default — fill in only what you use.
#
# Provider selection is cloud-first (claude-code, then mistral) with a local
# ollama fallback (the agent runs `ollama serve` on demand). Force one with:
# provider=claude-code   # or: mistral | ollama
#
# --- Mistral (cloud) ---
# MISTRAL_API_KEY=
# MISTRAL_MODEL=mistral-large-latest
#
# --- Ollama (local fallback) ---
# OLLAMA_HOST=http://localhost:11434
# OLLAMA_MODEL=llama3.1
#
# --- claude-code (cloud, uses your existing `claude` CLI login; no key needed here) ---
# CLAUDE_CODE_CMD=claude -p
